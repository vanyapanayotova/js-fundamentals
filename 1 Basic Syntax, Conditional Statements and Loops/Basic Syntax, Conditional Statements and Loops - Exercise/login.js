function login(arr) {
    let user = arr[0];

    for (let pass = 1; pass <= arr.length; pass++) {
       if (pass !== user.reverse) {
        
       
       }  
    }
}
login(['Acer','login','go','let me in','recA']);